# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.
#
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

meteor-base             # Packages every Meteor app needs to have
mobile-experience       # Packages for a great mobile UX
mongo                   # The database Meteor supports right now
blaze-html-templates    # Compile .html files into Meteor Blaze views
reactive-var            # Reactive variable for tracker
jquery                  # Helpful client-side library
tracker                 # Meteor's client-side reactive programming library

standard-minifier-css   # CSS minifier run for production mode
standard-minifier-js    # JS minifier run for production mode
es5-shim                # ECMAScript 5 compatibility for older browsers.
ecmascript              # Enable ECMAScript2015+ syntax in app code

autopublish             # Publish all data to the clients (for prototyping)
insecure                # Allow all DB writes from clients (for prototyping)
accounts-ui             # Simple templates to add login widgets to an app
accounts-password       # Password support for accounts
accounts-google         # Login service for Google accounts
practicalmeteor:mocha
xolvio:cleaner
velocity:meteor-stubs
practicalmeteor:chai
session                 # Session variable
mdg:camera              # Photos with one function call on desktop and mobile.
gadicc:blaze-react-component      # Use Blaze templates inside of React

service-configuration   # Manage the configuration for third-party services
kadira:flow-router
blaze                   # Meteor Reactive Templating library
twbs:bootstrap          # The most popular front-end framework for developing responsive, mobile first projects on the web.  
edgee:slingshot         # Directly post files to cloud storage services, such as AWS-S3.
dburles:google-maps     # Google Maps Javascript API v3
react-meteor-data       # React mixin for reactively tracking Meteor data (needed for maps)
mdg:geolocation         # Provides reactive geolocation on desktop and mobile.
check                   # Check whether a value matches a pattern
